<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.barkitapp.android.PlacesFragment">

    <android.support.design.widget.AppBarLayout
        android:id="@+id/appbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar">

        <android.support.v7.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="?attr/colorPrimary"
            app:popupTheme="@style/ThemeOverlay.AppCompat.Light" />

    </android.support.design.widget.AppBarLayout>
<RelativeLayout
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:layout_below="@id/appbar"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView android:text="Featured"
        android:id="@+id/featuredTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingTop="8dp"
        android:paddingBottom="16dp"
        android:textColor="@color/secondary_text"
        android:textSize="16sp"/>

    <android.support.v7.widget.RecyclerView xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/featuredList"
        android:layout_below="@id/featuredTitle"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:isScrollContainer="false"
        android:paddingTop="8dp"
        android:clipToPadding="false"
        android:paddingBottom="64dp"/>

    <TextView android:text="My Places"
        android:id="@+id/featuredMyPlaces"
        android:layout_below="@id/featuredList"
        android:layout_width="wrap_content"
        android:paddingTop="32dp"
        android:paddingBottom="16dp"
        android:layout_height="wrap_content"
        android:textColor="@color/secondary_text"
        android:textSize="16sp"/>

    <android.support.v7.widget.RecyclerView xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/myPlacesList"
        android:layout_below="@id/featuredMyPlaces"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:isScrollContainer="false"
        android:paddingTop="8dp"
        android:clipToPadding="false"
        android:paddingBottom="64dp"/>

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_marginTop="6dp"
        android:layout_marginBottom="6dp"
        android:layout_marginRight="6dp"
        android:src="@drawable/ic_add_white_48dp" />
</RelativeLayout>

</RelativeLayout>
